const express = require('express'); // Import express

const app = express(); // Make an instance of express

app.listen(5000 , console.log("Server is running on port 5000")); // Make the server on port 5000 (you can enter any port you want )